export const ColoredButton = ({ className = '', backgroundColor, borderColor }) => (
	<a
		className={`block border-2 max-w-max px-3 py-2 rounded ${className}`}
		href="https://github.com/QuentinFrc/docs"
		style={{ backgroundColor, borderColor }}>
		<span className="block">Votez ici</span>
	</a>
);
